import Autoform.SpecsGen.Basis
import Autoform.Harness.Audit
import Autoform.Generated.V8Base
import Autoform.SpecsGen.V8Base.Base

set_option maxRecDepth 20000
set_option maxHeartbeats 1000000

open Autoform.Core Autoform.Refine Autoform.SpecsGen
open Autoform.SpecsGen.V8Base.Base
open Autoform.Generated.V8Base

namespace Autoform.SpecsGen.V8Base.Part73

def ob_charact_v8_base_MulWithWraparound_int16_t_int16_t_int16_t : Prop :=
  ∀ fuel, FUEL ≤ fuel → ((dom_charact_v8_base_MulWithWraparound_int16_t_int16_t_int16_t).all (lawConform C fuel f_v8_base_MulWithWraparound_int16_t_int16_t_int16_t_)) = true


-- Holds at `FUEL` by kernel computation. The forall-fuel transport is recorded as
-- ob_charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot rather than proved; see the module header for the measured cost.
-- Not settled by computation here (evaluation did not terminate within the
-- heartbeat budget). Recorded as an obligation rather than asserted.
def ob_charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot_at_FUEL : Prop := ((dom_charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot).all (lawConform C FUEL f_v8_base_Stack_GetRealStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot_)) = true

def ob_charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot : Prop :=
  ∀ fuel, FUEL ≤ fuel → ((dom_charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot).all (lawConform C fuel f_v8_base_Stack_GetRealStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot_)) = true


/-- Everything this module states but does not prove. A `Prop`-valued `def` asserts nothing, so nothing here is admitted. -/
def obligations : List OpenObligation :=
  []

#eval IO.println (renderObligations "V8Base" obligations)

/-! ## Anti-vacuity gate

`#audit_depends` fails the build if a theorem's proof term never mentions the generated definition it claims to be about — the necessary half of the gate. The sufficient half is `scripts/mutate.py`, run against this file. -/

-- (audit dropped: `runs_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_debug_EnableInProcessStackDumping_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_debug_BacktraceOutputHandler_OutputFileDescriptor_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_EmulatedVirtualAddressSubspace_CanAllocateSubspaces_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_FPU_GetFlushDenormals_bool` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_SetPrintStackTrace_void_void` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_SetFatalFunction_void_void` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemoryMapping_GetMemory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemory_GetMemory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemory_GetSize_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_PageAllocator_CanAllocateSharedPages_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_0_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_CanReserveAddressSpace_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_PosixInitializeCommon_void_char` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_IsHardwareEnforcedShadowStacksEnabled_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_duplicate_0_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_1_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_CanReserveAddressSpace_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_1_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_memory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DirectorySeparator_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_n_FreeSubReservation_duplicate_0_bool_v8_base_AddressSpaceReservation` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_duplicate_0_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DiscardSystemPages_duplicate_2_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DirectorySeparator_duplicate_1_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_duplicate_1_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_2_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_3_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Win32MemoryMappedFile_memory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Win32MemoryMappedFile_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_n_FreeSubReservation_duplicate_1_bool_v8_base_AddressSpaceReservation` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_duplicate_1_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_4_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_memory_duplicate_0_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_size_duplicate_0_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_TimeTicks_IsHighResolution_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_ThreadTicks_IsSupported_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSpacePageAllocator_SealPages_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_VirtualAddressSubspace_ActiveMemoryProtectionKey_optional` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressRegion_begin_Address___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressRegion_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_BoundedPageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_BoundedPageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_location_status_v8_base_BoundedPageAllocator_AllocationStatus___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_vendor_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_stepping_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_model_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_ext_model_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_family_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_ext_family_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_type_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_implementer_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_architecture_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_variant_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_part_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fpu_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_icache_line_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_dcache_line_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cmov_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sahf_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mmx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_ssse3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse41_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse42_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_osxsave_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_vnni_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_vnni_int8_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fma3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_f16c_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_bmi1_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_bmi2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lzcnt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_popcnt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_apx_f_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx10_1_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_atom_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_intel_jcc_erratum_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cetss_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_non_stop_time_stamp_counter_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_running_in_vm_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_idiva_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_neon_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_thumb2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp3_d32_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_jscvt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_dot_prod_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lse_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mte_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sha3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_pmull1q_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fp16_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_hbc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cssc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mops_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sve_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_svebitperm_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_fp64_mode_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_msa_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_vlen_unsigned_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_rvv_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zba_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zbb_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zbs_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zfa_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_rvc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zfh_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zvfh_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_riscv_mmu_v8_base_CPU_RV_MMU_MODE___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lsx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lasx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_EmulatedVirtualAddressSubspace_mapped_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_Hasher___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_Hasher___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Hasher_hash_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_hash_value_size_t_unsigned_char` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_hash_value_size_t_shortunsigned` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_DiyFp_f_uint64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_DiyFp_e_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Double_AsUint64_uint64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `identity_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `involutive_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_internal_RangeCheck_IsOverflowFlagSet_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_internal_RangeCheck_IsUnderflowFlagSet_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_IsRemapPageSupported_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressSpaceReservation_base_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressSpaceReservation_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_name_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_stack_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_priority_v8_base_Thread_Priority___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_name_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_data_v8_base_Thread_PlatformData` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_priority_v8_base_Thread_Priority___const` is now an obligation, not a theorem)
-- (audit dropped: `identity_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `involutive_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Semaphore_native_handle_NativeHandle` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Semaphore_native_handle_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_time_internal_TimeBase_ToInternalValue_int64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_ase_RegionAllocator_Region_state_v8_base_RegionAllocator_RegionState` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RegionAllocator_free_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RegionAllocator_page_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_LsanPageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_LsanPageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RandomNumberGenerator_initial_seed_int64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_RandomNumberGenerator_min_unsigned_int` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_begin_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_cbegin_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_data_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_OwnedVector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_EmbeddedVector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSpace_SetName_bool_std_string` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSubspace_CanAllocateSubspaces_bool` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_SetPrintStackTrace_void_void` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_SetFatalFunction_void_void` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_PosixInitializeCommon_void_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash_value_size_t_unsigned_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash_value_size_t_shortunsigned` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)

/-! ## Axiom basis

`#audit_axioms` fails the build on `sorryAx`, `ofReduceBool` or `ofReduceNat`, so "no admitted step, no `native_decide`" is checked here rather than asserted in prose. -/

-- (audit dropped: `runs_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_debug_EnableInProcessStackDumping_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_debug_BacktraceOutputHandler_OutputFileDescriptor_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_EmulatedVirtualAddressSubspace_CanAllocateSubspaces_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_FPU_GetFlushDenormals_bool` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_SetPrintStackTrace_void_void` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_SetFatalFunction_void_void` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemoryMapping_GetMemory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemory_GetMemory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_SharedMemory_GetSize_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_PageAllocator_CanAllocateSharedPages_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_0_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_CanReserveAddressSpace_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_PosixInitializeCommon_void_char` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_IsHardwareEnforcedShadowStacksEnabled_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_duplicate_0_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_1_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_CanReserveAddressSpace_duplicate_0_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_1_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_memory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DirectorySeparator_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_n_FreeSubReservation_duplicate_0_bool_v8_base_AddressSpaceReservation` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_duplicate_0_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DiscardSystemPages_duplicate_2_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_DirectorySeparator_duplicate_1_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SetMemoryRegionName_duplicate_1_bool_void__size_t_char` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_SealPages_duplicate_2_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_3_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Win32MemoryMappedFile_memory_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Win32MemoryMappedFile_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_n_FreeSubReservation_duplicate_1_bool_v8_base_AddressSpaceReservation` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_ActivationFrameAlignment_duplicate_1_int` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_HasLazyCommits_duplicate_4_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_memory_duplicate_0_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PosixMemoryMappedFile_size_duplicate_0_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_TimeTicks_IsHighResolution_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_ThreadTicks_IsSupported_bool` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSpacePageAllocator_SealPages_bool_void__size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_VirtualAddressSubspace_ActiveMemoryProtectionKey_optional` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_AddressRegion___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressRegion_begin_Address___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressRegion_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_BoundedPageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_BoundedPageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_location_status_v8_base_BoundedPageAllocator_AllocationStatus___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_vendor_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_stepping_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_model_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_ext_model_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_family_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_ext_family_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_type_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_implementer_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_architecture_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_variant_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_part_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fpu_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_icache_line_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_dcache_line_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cmov_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sahf_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mmx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_ssse3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse41_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sse42_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_osxsave_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_vnni_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx_vnni_int8_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fma3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_f16c_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_bmi1_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_bmi2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lzcnt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_popcnt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_apx_f_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_avx10_1_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_atom_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_intel_jcc_erratum_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cetss_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_non_stop_time_stamp_counter_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_running_in_vm_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_idiva_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_neon_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_thumb2_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_vfp3_d32_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_jscvt_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_dot_prod_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lse_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mte_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sha3_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_pmull1q_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_fp16_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_hbc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_cssc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_mops_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_sve_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_svebitperm_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_is_fp64_mode_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_msa_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_vlen_unsigned_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_rvv_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zba_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zbb_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zbs_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zfa_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_rvc_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zfh_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_zvfh_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_riscv_mmu_v8_base_CPU_RV_MMU_MODE___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lsx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_CPU_has_lasx_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_EmulatedVirtualAddressSubspace_mapped_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_Hasher___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_Hasher___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Hasher_hash_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_hash_value_size_t_unsigned_char` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_hash_value_size_t_shortunsigned` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_DiyFp_f_uint64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_DiyFp_e_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Double_AsUint64_uint64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `identity_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `involutive_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `heappure_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_internal_RangeCheck___init_1` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_internal_RangeCheck_IsOverflowFlagSet_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_internal_RangeCheck_IsUnderflowFlagSet_bool___const` is now an obligation, not a theorem)
-- (audit dropped: `runs_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `returns_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `commutes_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_PageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_OS_IsRemapPageSupported_bool` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressSpaceReservation_base_void____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_AddressSpaceReservation_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_name_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_stack_size_int___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_Options_priority_v8_base_Thread_Priority___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_name_char____const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_data_v8_base_Thread_PlatformData` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Thread_priority_v8_base_Thread_Priority___const` is now an obligation, not a theorem)
-- (audit dropped: `identity_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `idempotent_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `involutive_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Semaphore_native_handle_NativeHandle` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Semaphore_native_handle_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_time_internal_TimeBase_ToInternalValue_int64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_ase_RegionAllocator_Region_state_v8_base_RegionAllocator_RegionState` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RegionAllocator_free_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RegionAllocator_page_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_LsanPageAllocator_AllocatePageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_LsanPageAllocator_CommitPageSize_size_t` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_RandomNumberGenerator_initial_seed_int64_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_RandomNumberGenerator_min_unsigned_int` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_begin_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_cbegin_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_Vector_data_ANY___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_OwnedVector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uproj_v8_base_EmbeddedVector_size_size_t___const` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSpace_SetName_bool_std_string` is now an obligation, not a theorem)
-- (audit dropped: `uconst_v8_base_VirtualAddressSubspace_CanAllocateSubspaces_bool` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_bits_SignedMod32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_bits_SignedMod64_int64_t_int64_t_int64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_SetPrintStackTrace_void_void` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_SetFatalFunction_void_void` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_PosixInitializeCommon_void_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_OS_Initialize_duplicate_0_void_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_AddressRegion___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedDiv32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedDiv64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedMod32_uint32_t_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_UnsignedMod64_uint64_t_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_WraparoundAdd32_int32_t_int32_t_int32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_Hasher___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash64_uint64_t_uint64_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash32_uint32_t_uint32_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash_value_size_t_unsigned_char` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_hash_value_size_t_shortunsigned` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_internal_MakeStrictNum_StrictNumeric_T` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_internal_RangeCheck___init` is now an obligation, not a theorem)
-- (audit dropped: `charact_v8_base_MulWithWraparound_int16_t_int16_t_int16_t` is now an obligation, not a theorem)
-- (audit dropped: `charact_alStackAddressForSlot_v8_base_Stack_StackSlot_v8_base_Stack_StackSlot` is now an obligation, not a theorem)



end Autoform.SpecsGen.V8Base.Part73
